# Copy this file to .env.local and fill in the values.

# URL of the RealAI backend (default: Render deployment)
NEXT_PUBLIC_REALAI_API_BASE="https://realai-qz3b.onrender.com"

# Optional: server-side API key forwarded to the backend.
# If set here, the frontend never exposes it to the browser.
REALAI_API_KEY=""
