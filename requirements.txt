# RealAI Requirements
# Core packages – required for the web UI and chat completions (Vercel-compatible)
requests>=2.28.0
beautifulsoup4>=4.12.0

# Optional features – install manually for local / full-featured use:
#   pip install sentence-transformers  # semantic search / embeddings
#   pip install pyttsx3                # text-to-speech
#   pip install vosk                   # speech recognition
#   pip install web3                   # blockchain / Web3 integration
